# Activated only when the "json" profile is active (SPRING_PROFILES_ACTIVE=json).
# Switches console output to structured JSON for cloud log aggregators. The default
# profile (no profile) keeps the human-readable plain-text console output.
logging.structured.format.console=org.mobilitydata.gtfsvalidator.api.logging.JsonLogFormatter
